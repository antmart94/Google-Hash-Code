

import pandas as pd
import re

class Lib:

    def __init__(self,  libId, bookSet,  signUp,  scan):
            self.libId = libId
            self.bookSet = bookSet
            self.signUp = signUp
            self.scan = scan

